import React from "react"
import CreateButton from "./TdlCard/CreateCardButton"
// Always start your component name with CAPS, if not react will consider it as a HTML Tag

function main(){

    return(
        <div className="main">
            <CreateButton />
        </div>
    )
}

export default main;